#include <bits/stdc++.h>
#include "gift.h"
#include "couple.h"
using namespace std;
int main(int argc, char const *argv[])
{
	int r;
	//std::cout << "Enter 1 for default allocation of gifts and 2 for new algorithm\n";
	//std::cin >> r;
		cout << "Default allocation of gifts\n";
		int z = sum(8);
		cout << "\nNew allocation of gifts\n";
		z = sum2(8);
	
	return 0;
}